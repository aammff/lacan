analysand, invalid
analyst, context
psychoanalysis, invalid context

analyst = sinthome, tail recursion modulo cons
  where cons = call with current continuation

psychoanalytical text, serialized current continuation
